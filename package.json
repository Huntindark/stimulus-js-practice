{
  "name": "stimulus-js",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "16.x",
    "yarn": "1.22.x"
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=public/bundle.js",
    "watch": "esbuild src/index.js --bundle --outfile=public/bundle.js --watch"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.0.1",
    "esbuild": "^0.14.10",
    "watch-http-server": "^0.7.6"
  }
}
